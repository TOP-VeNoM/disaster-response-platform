{
  "name": "disaster-response-platform",
  "version": "1.0.0",
  "description": "Agentic disaster response platform with RAG-backed SOP retrieval and human-in-the-loop approval",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "seed": "node scripts/seedDatabase.js",
    "ingest:sops": "node scripts/ingestSOPs.js",
    "setup": "node scripts/setupDev.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
